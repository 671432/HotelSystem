<Window x:Class="WPFReceptionist.ServiceRoom"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFReceptionist"
        mc:Ignorable="d"
        Title="ServiceRoom" Height="446" Width="358">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF48AF9F"/>
                <GradientStop Color="#FFFD87FE" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <ListView x:Name="RoomListView" ItemsSource="{Binding}" MouseDoubleClick="RoomListView_MouseDoubleClick" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
            <ListView.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF00FFF9"/>
                    <GradientStop Color="#FFAB00FF" Offset="1"/>
                </LinearGradientBrush>
            </ListView.Background>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Room Number" DisplayMemberBinding="{Binding RoomNr}" Width="100"/>
                    <GridViewColumn Header="Beds" DisplayMemberBinding="{Binding Beds}" Width="50"/>
                    <GridViewColumn Header="Quality" DisplayMemberBinding="{Binding Quality}" Width="70"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="90"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
